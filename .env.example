# Pay4Edge Frontend — API and Socket
# Copy to .env.local (Vite loads .env.local automatically). Do not commit .env.local.

# Backend API base URL (must end with /api/v1 for HTTP calls)
VITE_API_URL=http://localhost:5000/api/v1

# Production example:
# VITE_API_URL=https://api.pay4edge.com/api/v1
